import { FilterCategoriasPipe } from './filter-categorias.pipe';

describe('FilterCategoriasPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCategoriasPipe();
    expect(pipe).toBeTruthy();
  });
});
