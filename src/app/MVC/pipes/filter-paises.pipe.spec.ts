import { FilterPaisesPipe } from './filter-paises.pipe';

describe('FilterPaisesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPaisesPipe();
    expect(pipe).toBeTruthy();
  });
});
