import { FilterUsuariosPipe } from './filter-usuarios.pipe';

describe('FilterUsuariosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUsuariosPipe();
    expect(pipe).toBeTruthy();
  });
});
