import { FilterPrestamosPipe } from './filter-prestamos.pipe';

describe('FilterPrestamosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPrestamosPipe();
    expect(pipe).toBeTruthy();
  });
});
