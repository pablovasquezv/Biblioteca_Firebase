import { FilterEditorialesPipe } from './filter-editoriales.pipe';

describe('FilterEditorialesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEditorialesPipe();
    expect(pipe).toBeTruthy();
  });
});
