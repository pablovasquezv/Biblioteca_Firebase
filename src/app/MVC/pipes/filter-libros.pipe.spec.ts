import { FilterLibrosPipe } from './filter-libros.pipe';

describe('FilterLibrosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLibrosPipe();
    expect(pipe).toBeTruthy();
  });
});
